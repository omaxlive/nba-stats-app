import { StatsFilterByIndexPipe } from './stats-filter-by-index.pipe';

describe('StatsFilterByIndexPipe', () => {
  it('create an instance', () => {
    const pipe = new StatsFilterByIndexPipe();
    expect(pipe).toBeTruthy();
  });
});
